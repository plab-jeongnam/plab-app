import Link from "next/link"

import { Button } from "@/components/ui/button"
import {
  Card,
  CardContent,
  CardDescription,
  CardHeader,
  CardTitle,
} from "@/components/ui/card"

export default function HomePage() {
  return (
    <main className="container mx-auto py-10 px-4">
      <div className="flex flex-col items-center justify-center gap-8">
        <div className="text-center space-y-4">
          <h1 className="text-4xl font-bold tracking-tight"><% .ProjectName %></h1>
          <p className="text-muted-foreground text-lg max-w-xl">
            plab-app으로 생성된 프로젝트에요.
            이 파일을 수정해서 시작해 보세요!
          </p>
        </div>

        <div className="grid gap-6 md:grid-cols-2 lg:grid-cols-3 w-full max-w-5xl">
          <Card>
            <CardHeader>
              <CardTitle>Tailwind CSS + shadcn/ui</CardTitle>
              <CardDescription>모던한 UI 컴포넌트</CardDescription>
            </CardHeader>
            <CardContent>
              <p className="text-sm text-muted-foreground">
                Tailwind CSS v4와 shadcn/ui를 사용하여 아름다운 UI를 빠르게 구축할 수 있습니다.
              </p>
            </CardContent>
          </Card>

          <Card>
            <CardHeader>
              <CardTitle>Zustand + React Query</CardTitle>
              <CardDescription>상태 관리</CardDescription>
            </CardHeader>
            <CardContent>
              <p className="text-sm text-muted-foreground">
                클라이언트 상태는 Zustand, 서버 상태는 React Query로 관리합니다.
              </p>
            </CardContent>
          </Card>

          <Card>
            <CardHeader>
              <CardTitle>TypeScript + Vitest</CardTitle>
              <CardDescription>타입 안전성과 테스트</CardDescription>
            </CardHeader>
            <CardContent>
              <p className="text-sm text-muted-foreground">
                TypeScript와 Vitest로 안정적인 개발 환경을 제공합니다.
              </p>
            </CardContent>
          </Card>
        </div>

        <div className="flex gap-4">
          <Button asChild>
            <Link href="/form">폼 예제 보기</Link>
          </Button>
<% if .UsePlabData %>
          <Button variant="secondary" asChild>
            <Link href="/plab-demo">플랩 데이터 데모</Link>
          </Button>
<% end %><% if .ResearchersOnly %>
          <Button variant="outline" asChild>
            <Link href="/protected">리서처 전용 페이지</Link>
          </Button>
<% end %>
        </div>
      </div>
    </main>
  )
}
