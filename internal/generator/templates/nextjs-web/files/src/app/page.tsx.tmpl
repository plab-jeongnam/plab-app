import Link from "next/link"

import { Button } from "@/components/ui/button"

export default function HomePage() {
  return (
    <main className="container mx-auto px-4 py-16">
      <div className="flex flex-col items-center justify-center gap-10 text-center">
        <div className="space-y-4">
          <h1 className="text-4xl font-bold tracking-tight"><% .ProjectName %></h1>
          <p className="max-w-xl text-lg text-muted-foreground">
            프로젝트가 준비됐어요.
          </p>
        </div>

        <div className="max-w-2xl space-y-3">
          <p className="text-2xl font-semibold">
            이제 만들고 싶은 걸 마음껏 만들어 보세요
          </p>
          <p className="text-base text-muted-foreground leading-relaxed">
            떠오르는 아이디어가 있나요? AI에게 얘기해 주세요.
            페이지를 더 만들거나, 디자인을 바꾸거나, 새로운 기능을 추가하는 것
            전부 대신 해드릴 수 있어요.
          </p>
          <p className="text-sm text-muted-foreground">
            예: <span className="italic">&quot;메인 화면에 경기 일정 리스트를 보여줘&quot;</span>,
            <span className="italic">&quot;다크 모드 버튼 추가해줘&quot;</span>,
            <span className="italic">&quot;색상을 플랩 브랜드 컬러로 바꿔줘&quot;</span>
          </p>
        </div>

        <div className="flex flex-wrap justify-center gap-3">
          <Button asChild>
            <Link href="/form">폼 예제 보기</Link>
          </Button>
<% if .UsePlabData %>
          <Button variant="secondary" asChild>
            <Link href="/plab-demo">플랩 데이터 데모</Link>
          </Button>
<% end %><% if .ResearchersOnly %>
          <Button variant="outline" asChild>
            <Link href="/protected">리서처 전용 페이지</Link>
          </Button>
<% end %>
        </div>
      </div>
    </main>
  )
}
